spring:
  application:
    name: ssz-service-product
  profiles:
    active: dev
  main:
    allow-bean-definition-overriding: true

server:
  port: 1001
#开启feign对sentinel的支持
feign:
  sentinel:
    enabled: true

